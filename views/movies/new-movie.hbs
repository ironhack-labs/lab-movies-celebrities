<h2 class="mb-3">New Movie</h2>

{{#if errors}}
  <div class="alert alert-danger">
    <ul>
      {{#each errors as |error|}}
        <li>{{error}}</li>
      {{/each}}
    </ul>
  </div>
{{/if}}

<form method="POST" action="/new-movie">
  <div class="form-group mb-2">
    <label>Title</label>
    <input name="title" type="text" class="form-control {{#if errors.title}}is-invalid{{/if}}" />
    <small class="invalid-feedback">{{errors.title}}</small>
  </div>

  <div class="form-group">
    <label>Genre</label>
    <input name="genre" type="text" class="form-control {{#if errors.genre}}is-invalid{{/if}}" />
    <small class="invalid-feedback">{{errors.genre}}</small>
  </div>

  <div class="form-group">
    <label>Plot</label>
    <input name="plot" type="text" class="form-control {{#if errors.plot}}is-invalid{{/if}}" />
    <small class="invalid-feedback">{{errors.plot}}</small>
  </div>

<div class="form-group">
<select class="form-select" multiple aria-label="multiple select example" name="cast">
    {{#each celebrities as |celebrity|}}
  <option value="{{celebrity.id}}">{{celebrity.name}}</option>
    {{/each}}
</select>
</div>

  <div class="d-flex mt-5">
    <button type="submit" class="btn btn-primary me-2">Submit</button>
  </div>
</form>
<h2>Movies</h2>

{{#each movies as |movie|}}
<a href="/movies/{{ _id }}">{{ title }}</a>
  {{#if movie.cast}}
    {{#each movie.cast as |celebrity|}}
      {{celebrity.name}}

    {{/each}}
  {{else}}
    <p>No cast</p>
  {{/if}}

{{/each}}
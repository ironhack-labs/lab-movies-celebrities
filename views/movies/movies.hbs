<h2>Movies</h2>
<div class="row">
    {{#each movies}}
    <div class="class col-xl-3 display: flex, justify-content: center">
        <h1>
            <h3>Película: {{title}}</h3>
            <h4>Género : {{genre}}</h4>
            <h5>Tipo: {{plot}}</h5>
            <h5>Actores : {{#each cast}} {{name}} {{/each}} </h5>
            <p>
                <a href="/movies/{{_id}}">Detalles</a>
            </p>
            {{/each}}
            {{!-- <a href="/drones/{{_id}}/edit" class="btn btn-dark">Update</a>
            <form action="/drones/{{_id}}/delete" method="POST">
                <input type="submit" value="Eliminar Drone" class="btn btn-danger">
            </form> --}}

            <hr>
        </h1>
    </div>
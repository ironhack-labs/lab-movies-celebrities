<div class="container">

    <div class="row justify-content-center">
        <div class="col-md-6">

            <h2>Edita {{movies.title}}</h2>

            <form action="/movies/{{movies._id}}/edit" method="POST">
                <div class="mb-3">
                    <label for="title" class="form-label">Título</label>
                    <input type="text" class="form-control" id="title" name="title" value="{{movies.title}}">
                </div>
                <div class="mb-3">
                    <label for="genre" class="form-label">Género</nav></label>
                    <input type="text" class="form-control" id="genre" name="genre" value="{{movies.genre}}">
                </div>
                <div class="mb-3">
                    <label for="plot" class="form-label">Gráfico</label>
                    <input type="text" class="form-control" id="plot" name="plot" value="{{movies.plot}}">
                </div>
                <div class="mb-3">
                    <label for="cast" class="form-label">Actores/Celebritis</label>

                    <select multiple class="form-select" name="cast">
                        {{#each celebritiesFromDB}}
                        <option value="{{_id}}">{{name}}</option>
                        {{/each }}

                    </select>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-dark">Editar Película</button>
                </div>

            </form>

            <hr>
        </div>
    </div>



    <a href="/" class="btn btn-dark">Inicio</a>
</div>
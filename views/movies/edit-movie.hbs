<div class="container">

    <div class="row">

        <div class="col-md-6">
            <h2>Edit a Movie</h2>
            <form action="/movies/create" method="POST">
                <div class="mb-3">
                    <label for="name" class="form-label">Title</label>
                    <input type="text" class="form-control" id="title" name="title" value="{{info.[0].title}}">
                </div>

                <div class="mb-3">
                    <label for="propellers" class="form-label">Genre</label>
                    <input type="text" class="form-control" id="genre" name="genre" value="{{info.[0].genre}}">
                </div>

                <div class="mb-3">
                    <label for="maxSpeed" class="form-label">Plot</label>
                    <input type="text" class="form-control" id="plot" name="plot" value="{{info.[0].plot}}">
                </div>

                <div class="mb-3">
                    <label for="cast" class="form-label">Cast <span>CTRL for multiple choice</span></label>
                    <select class="form-select" name="cast" id="cast" multiple>
                        {{#each info.[1]}}
                        <option value="{{id}}">{{name}}</option>
                        {{/each}}
                    </select>
                </div>

                <button type="submit" class="btn btn-dark">EDIT</button>
            </form>
        </div>

    </div>

</div>
<section class="list">
    <div class="container">
        <h2>PELÍCULAS</h2>

        <ol class="list-group list-group-numbered">
            {{#each movies}}
            <li class="list-group-item d-flex justify-content-between align-items-start">
                <div class="ms-2 me-auto">
                    <div class="fw-bold">
                        <a href="/movies/{{_id}}">{{title}}</a>
                    </div>
                    {{#each cast}}
                    {{name}}
                    {{/each}}
                </div>
                <span class="badge rounded-pill" style="background-color: #f05811;">{{genre}}</span>
            </li>
            {{/each}}
        </ol>
    </div>
</section>
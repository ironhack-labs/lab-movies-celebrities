<a href="/">Home</a>

<h2>Movie Details</h2>

<div class="container49">
    <h3>{{movie.title}}</h3>
    <h4>Genre: </h4><h5>{{movie.genre}}</h5>    
    <h4>Plot: </h4><h5>{{movie.plot}}</h5>
    <h4>Cast: 
        {{#each movie.cast}}
            <h5>{{this.name}}, {{this.occupation}}, {{this.catchPhrase}}</h5>
        {{/each}}
    </h4>
    <a href="/movies/{{movie._id}}/edit">Edit</a>
    <form action="/movies/{{movie._id}}/delete" method="post">
        <button type="submit">Delete</button>
    </form>
</div>